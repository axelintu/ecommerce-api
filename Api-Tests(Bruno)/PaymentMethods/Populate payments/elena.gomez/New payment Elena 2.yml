info:
  name: New payment Elena 2
  type: http
  seq: 2

http:
  method: POST
  url: "{{APIaddress}}/{{endpoint}}"
  body:
    type: json
    data: |-
      {
        "type": "bank_transfer",
        "bankName": "Banco Santander",
        "accountNumber": "ES8220385778983000760337",
        "isDefault": false,
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
