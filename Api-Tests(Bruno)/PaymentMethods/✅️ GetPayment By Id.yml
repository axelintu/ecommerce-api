info:
  name: ✅️ GetPayment By Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{APIaddress}}/{{endpoint}}/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
