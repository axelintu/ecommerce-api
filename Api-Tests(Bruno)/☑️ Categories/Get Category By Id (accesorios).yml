info:
  name: Get Category By Id (accesorios)
  type: http
  seq: 2

http:
  method: GET
  url: "{{APIaddress}}/{{endpoint}}/{{accesorios}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
