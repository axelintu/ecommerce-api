info:
  name: "New: Tour Merch (parent: merch-official)"
  type: http
  seq: 4

http:
  method: POST
  url: "{{APIaddress}}/{{endpoint}}"
  body:
    type: json
    data: |-
      {
        "urlName": "tour-merch",
        "name": "Tour Merch",
        "description": "Merch exclusiva vendida en los tours de tus artistas favoritos. Encuentra abanicos, medallas y accesorios limitados de tus giras favoritas.",
        "imageURL": "https://placehold.co/800x600.png",
        "parentCategory": "{{cat_merchoficial}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const id = res.getBody()._id;
        bru.setEnvVar("cat_tourmerch", id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  This is a POST request, submitting data to an API via the request body. This request submits JSON data, and the data is reflected in the response.

  A successful POST request typically returns a `200 OK` or `201 Created` response code.
