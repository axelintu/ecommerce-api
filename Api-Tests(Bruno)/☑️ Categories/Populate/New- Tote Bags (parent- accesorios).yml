info:
  name: "New: Tote Bags (parent: accesorios)"
  type: http
  seq: 6

http:
  method: POST
  url: "{{APIaddress}}/artists"
  body:
    type: json
    data: |2-
        {
          "urlName": "tote-bags",
          "name": "Tote Bags",
          "description": "Bolsos de tela versátiles y funcionales con diseños exclusivos de tus artistas favoritos. Perfectos para llevar contigo a cualquier lugar.",
          "imageURL": "https://placehold.co/800x600.png",
            "parentCategory": {{accesorios}}
          }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        console.log(data);

        // Safety check: Ensure data exists and is an object
        if (data && typeof data === 'object') {
          const {_id} = data;

          bru.setVar("tote-bags", _id);
        } else {
          console.error("No data received in response body");
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  This is a POST request, submitting data to an API via the request body. This request submits JSON data, and the data is reflected in the response.

  A successful POST request typically returns a `200 OK` or `201 Created` response code.
