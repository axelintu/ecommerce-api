info:
  name: Login (NoAdmin)
  type: http
  seq: 1

http:
  method: POST
  url: "{{APIaddress}}/auth/login/"
  body:
    type: json
    data: |-
      {
          "email": "alejandro@floresprieto2.com",
          "password": "clavesimple"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        // Extract access_token from the JSON response body
        const token = res.getBody().token;

        // Save it to the active environment
        bru.setEnvVar("JWT", token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
