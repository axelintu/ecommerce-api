info:
  name: 🦃 Login ajolote@fa4.com
  type: http
  seq: 6

http:
  method: POST
  url: "{{APIaddress}}/auth/login/"
  body:
    type: json
    data: |-
      {
          "email": "ajolote@fa4.com",
          "password": "clavesimple"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        // Extract access_token from the JSON response body
        const token = res.getBody().token;

        // Save it to the active environment
        bru.setEnvVar("JWT", token, { persist: true });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
