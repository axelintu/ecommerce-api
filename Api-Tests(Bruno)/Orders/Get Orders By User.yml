info:
  name: Get Orders By User
  type: http
  seq: 1

http:
  method: GET
  url: "{{APIaddress}}/{{endpoint}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
