info:
  name: Delete Product
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{APIaddress}}/{{endpoint}}/:id"
  params:
    - name: id
      value: 69f39345ecc3638b442ed602
      type: path
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: toDelete
      value: 69f26899d075c554c978fdb4

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  This is a POST request, submitting data to an API via the request body. This request submits JSON data, and the data is reflected in the response.

  A successful POST request typically returns a `200 OK` or `201 Created` response code.
