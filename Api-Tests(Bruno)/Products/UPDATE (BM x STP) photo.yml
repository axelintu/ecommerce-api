info:
  name: UPDATE (BM x STP) photo
  type: http
  seq: 4

http:
  method: PUT
  url: "{{APIaddress}}/{{endpoint}}/:id"
  params:
    - name: id
      value: 69f2db13349ec6ab2482389d
      type: path
  body:
    type: json
    data: |
      {
        "imageURL": "/images/prods/babymetal/1764724545076/slaughter-to-prevail-x-babymetal-babyslaugher-bear-t-shirt-265761.png"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Successful POST request", function () {
            expect(res.getStatus()).to.be.oneOf([200, 201]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  This is a POST request, submitting data to an API via the request body. This request submits JSON data, and the data is reflected in the response.

  A successful POST request typically returns a `200 OK` or `201 Created` response code.
