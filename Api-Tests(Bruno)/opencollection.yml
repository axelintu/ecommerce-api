opencollection: 1.0.0

info:
  name: Fox of Wisteria Api Tests v2
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: bearer
    token: "{{JWT}}"
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
