info:
  name: By Id copy (Not the user's)
  type: http
  seq: 3

http:
  method: GET
  url: "{{APIaddress}}/{{}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
